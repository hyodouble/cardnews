# 카드뉴스 디자인 기준

레퍼런스를 뜯어보고 정리한 규칙. `make_cards.py`는 이 문서를 구현한 것으로 본다.

레퍼런스
- `@ai.trend.kr` 캐러셀 (instagram.com/p/DcfwLiAEb51) — 한국어 인스타 캐러셀의 완성형
- `paseo.sh` — 다크 테마 제품 랜딩의 타이포·여백 기준
- 릴스 (instagram.com/reel/DYEwJ_0x42G) — 2026-08-29 추가, **아직 분석 안 함.**
  아래 "미분석 레퍼런스" 참고

---

## 1. 다크가 기본이다

두 레퍼런스 모두 어두운 배경이다. paseo는 본문 배경이 `rgb(16, 22, 21)` — 순검정이 아니라
아주 살짝 초록이 도는 차콜이다. ai.trend도 순검정이 아닌 따뜻한 차콜을 쓴다.

순검정(`#000`)은 피한다. 화면에서 구멍처럼 보이고 대비가 과해서 눈이 아프다.
채도를 조금 실은 어두운 회색이 낫다.

현재 우리 카드는 크림 배경 + 오렌지다. 밝은 피드에서는 잘 보이지만,
인스타 피드는 대부분 밝은 이미지라 **다크 카드가 스크롤 중에 더 튄다.**
브랜드 오렌지는 유지하되 배경을 뒤집는 쪽을 검토할 것.

## 2. 강조색은 하나, 아껴 쓴다

ai.trend는 앰버 하나만 쓴다. 그것도 슬라이드당 두세 군데:
- 킥커 (`PLUGIN 02`)
- 헤드라인 안의 핵심 단어 한 덩어리
- 본문 안 굵은 구절 하나

나머지는 전부 흰색과 회색이다. **강조색이 많아지면 강조가 사라진다.**
우리 팔레트에서는 오렌지가 그 역할이다. 한 슬라이드에 세 곳 이상 쓰지 않는다.

## 3. 헤드라인 안에서 무게를 섞는다

ai.trend의 헤드라인은 한 줄 안에서 흰색 보통 + 앰버 굵게가 섞인다.

```
딱 봐도 AI 티 나는
디자인, 탈출법 5개      <- 뒷부분만 앰버 + 밑줄
```

한 덩어리를 통으로 굵게 칠하는 것보다, **문장에서 진짜 중요한 명사구 하나만**
색과 무게를 올리는 편이 훨씬 읽힌다. 밑줄은 그 위에 한 번 더 얹는 장치다.

우리 현재 헤드라인은 전부 같은 무게 같은 색이다. 개선 여지가 여기 있다.

## 4. 매 슬라이드에 고정 프레임을 둔다

ai.trend는 모든 슬라이드에 같은 위치, 같은 크기로:
- 좌상단: 점 + `AI TREND` (작은 대문자, 회색)
- 우상단: `02/ 07` — 현재 번호만 흰색 굵게, 나머지는 회색

이 프레임이 캐러셀을 한 벌로 묶는다. 내용이 바뀌어도 눈이 같은 자리에 앉는다.

우리는 하단에 핸들 + 카운터를 두고 있다. 방향은 같지만 **상단이 비어 있다.**
브랜드 마크를 좌상단으로 올리고 카운터를 우상단으로 올리면 프레임이 잡힌다.

## 5. 슬라이드마다 시각 요소가 하나씩 있다

ai.trend는 텍스트만 있는 슬라이드가 없다. 매번 뭔가 하나 있다:
- 도입/마무리: 기울어진 카드 5장을 부채꼴로 펼친 인덱스
- 각 플러그인: 실제 웹사이트 스크린샷을 브라우저 크롬(신호등 점 3개 + 주소창) 안에 넣은 목업

목업의 브라우저 크롬이 핵심이다. 스크린샷을 그냥 붙이면 이미지 조각처럼 보이는데,
**크롬을 씌우면 "실제 화면"이라는 신호**가 된다. 만들기도 쉽다 — 둥근 사각형,
점 세 개, 주소창 하나.

우리 카드는 전부 텍스트다. 이게 지금 가장 큰 격차다.

## 6. 여백은 크게, 글자는 작게

paseo의 h1은 48px에 line-height 48px — **행간이 글자 크기와 같다.**
자간은 `-1.2px`로 좁혔다. 큰 글자는 행간을 좁히고 자간을 조여야 덩어리로 읽힌다.

본문은 18px에 line-height 29.25px — 약 1.63배. 큰 글자와 반대로 넉넉하다.

정리하면: **헤드라인은 1.0~1.15배, 본문은 1.5~1.65배.**
우리는 헤드라인 1.18배, 본문 1.42배를 쓰고 있다. 헤드라인은 더 조이고
본문은 더 벌리는 게 맞다.

## 7. 본문 색은 흰색을 낮춘 것

paseo 본문은 흰색 70% 투명도다. 회색을 따로 고르지 않고 **같은 흰색의 농도만 낮췄다.**
그래서 배경과 자연스럽게 어울린다. 회색을 별도 색으로 고르면 대개 탁해진다.

## 8. 마지막 장은 버튼처럼 생겨야 한다

ai.trend의 마지막 슬라이드:
- 도입부에서 봤던 카드 부채꼴을 그대로 다시 보여준다 (수미상관)
- 그 아래 **알약 모양 버튼**에 앰버 배경 + 검정 글씨로 행동 문구 하나

버튼은 실제로 눌리지 않지만 눌러야 할 것처럼 생겼다. 그게 요점이다.
우리 CTA는 흰 알약에 오렌지 글씨다. 대비를 뒤집어 **오렌지 배경 + 어두운 글씨**로
가면 더 눌러 보인다.

## 9. 스와이프 유도는 명시적으로

ai.trend 1번 슬라이드 우하단에 `밀어서 확인하기 →`가 있다.
캐러셀의 최대 이탈 지점은 1→2장이다. 여기에 한 줄 넣는 비용은 없다.

우리 훅 슬라이드에는 이게 없다.

---

## 우리 카드에 적용할 것 (우선순위)

1. **슬라이드마다 시각 요소 하나** — 최소한 브라우저 크롬 목업, 큰 숫자, 아이콘 중 하나.
   현재 전부 텍스트인 게 가장 큰 약점
2. **헤드라인 안 부분 강조** — 핵심 명사구만 오렌지 + 굵게
3. **상단 고정 프레임** — 좌상단 브랜드, 우상단 카운터
4. **훅 슬라이드에 스와이프 유도** 한 줄
5. **다크 배경 검토** — 피드에서의 대비
6. **행간 조정** — 헤드라인 1.1배로 조이고 본문 1.6배로 벌리기
7. **CTA 버튼 대비 반전** — 오렌지 배경에 어두운 글씨

## 안 따라 할 것

ai.trend의 1번 슬라이드는 3D 렌더링 캐릭터다. 만들려면 이미지 생성이 필요하고
매번 톤을 맞추기 어렵다. 우리는 타이포와 도형으로 간다 — 재현 가능한 것만 쓴다.

---

# 2차 개정 (2026-08-28)

`@thenewsmovement`를 보고 나서 판단이 바뀐 부분. 앞의 규칙 중 일부를 뒤집는다.

## 박스와 구분선이 낡아 보이게 만든다

1차 개정에서 넣은 것들 — 상단 앰버 바, 헤드라인 밑 짧은 밑줄, 번호 칩, 좌우로 나눈 상단 프레임 —
이게 전부 "옛날 카드뉴스" 신호였다. 요즘 잘 나가는 계정은 **면과 타입만 쓴다.**

지운 것: 상단 컬러 바, 헤드라인 밑줄, 번호 칩, 고스트 숫자, 본문 위 구분선.

## 타입이 훨씬 커야 한다

레퍼런스의 헤드라인은 화면 폭을 꽉 채우는 **압축 산세리프 대문자**에 행간이 글자 크기보다 좁다.
우리는 74px에 행간 1.14였다. 지금은 **92px에 행간 0.94**, 폰트는 Arial Narrow Bold.

한글은 글자가 넓고 행간이 더 필요하다. 자동으로 **크기 0.74배, 행간 1.18배**로 바꾼다.
같은 값을 쓰면 한글이 화면을 뚫는다.

## 가운데가 아니라 아래에 붙인다

세로 가운데 정렬은 내용이 짧으면 허공에 뜬다. 레퍼런스는 전부 **하단 마진 기준 앵커**다.
훅·본문·CTA 모두 아래에서 쌓아 올린다.

## 스크림은 위가 아니라 아래

사진 위에 글을 얹을 때 상단을 어둡게 덮으면 사진의 좋은 부분이 죽는다.
**아래에서 위로 올라오는 그라디언트**를 쓰면 사진은 살고 글은 읽힌다.

## 단색 슬라이드를 섞는다

전부 같은 배경이면 캐러셀이 단조롭다. **3장마다 한 번 앰버 단색**으로 뒤집는다.
이때 대비는 색이 아니라 **무게**로 준다 — 앰버 위에서는 검정 본문에 강조만 완전한 검정,
헤드라인 강조는 흰색.

## 크롬은 작고 조용하게

브랜드는 **상단 가운데 아주 작게**, 카운터는 **우하단에 `01 — 10`** 형식으로.
1차 개정의 좌상단 점+브랜드, 우상단 큰 숫자는 존재감이 너무 컸다.

## 그레인

단색 면과 그라디언트에 노이즈를 아주 약하게 얹는다. 밴딩이 사라지고 인쇄물처럼 읽힌다.

## Gemini 워터마크

생성 이미지 우하단에 ✦ 마크가 박힌다. 지우지 말고 **프레임의 12%를 잘라낸다** —
복원 작업보다 깨끗하고 구도도 크게 안 상한다.
다만 SynthID 비가시 워터마크는 파일에 그대로 남는다. AI 이미지를 쓴다는 사실은 계정 소개나
캡션에 한 줄 밝혀두는 편이 낫다.

## 한국어판

같은 레이아웃으로 영어판과 한국어판을 함께 뽑는다. 한국어 원고는 콘텐츠 JSON의 슬라이드마다
`ko` 블록에 넣고, 없는 필드는 영어판으로 자동 폴백한다.

주의: Segoe UI에는 한글이 없다. 킥커·스와이프 문구 같은 작은 라벨까지 전부 폰트 폴백을
태워야 한다. 안 그러면 두부(□)가 뜬다.

# 3차 개정 (2026-08-28) — 서체

시스템 폰트를 버리고 오픈 서체를 저장소에 넣었다 (`fonts/`). 이게 이번 개정에서
디자인을 가장 크게 바꾼 요소다.

| 용도 | 이전 | 지금 |
|---|---|---|
| 영문 헤드라인 | Arial Narrow Bold | **Anton** |
| 한글 헤드라인 | 맑은 고딕 Bold | **Pretendard ExtraBold** |
| 본문 (양쪽) | Segoe UI / 맑은 고딕 | **Pretendard Regular / Medium** |

Arial Narrow는 압축 서체가 필요해서 쓴 임시방편이었다. Anton은 애초에 포스터용으로
그려진 서체라 같은 크기에서 훨씬 무겁고 균일하게 읽힌다. 다만 x-height가 커서
행간을 0.94에서 **1.02**로 풀어야 글자가 붙지 않는다.

Pretendard는 한글 UI의 사실상 표준이고 라틴 글리프도 같이 들고 있어서, 한글 본문과
영문 본문을 **하나의 서체로 통일**할 수 있다. 맑은 고딕과 Segoe UI를 섞던 때보다
두 언어판의 인상이 훨씬 가까워졌다.

한글 헤드라인은 여전히 라틴보다 작게 간다 — 크기 **0.72배**, 행간 **1.16배**.

---

# 미분석 레퍼런스

## instagram.com/reel/DYEwJ_0x42G — 2026-08-29 등록

레퍼런스로 쓰기로 한 릴스. 아직 화면을 못 봐서 규칙을 뽑지 못했다.
(작업 환경에서 인스타그램 도메인이 막혀 있어 링크만으로는 열 수 없다.
스크린샷이나 영상 파일이 리포에 들어오면 그때 뜯는다.)

볼 수 있게 되면 이 목록으로 뜯는다. 위 1~9번과 같은 층위로 정리해서
이 문서 본문에 올리고, 이 항목은 지운다.

- **커버 프레임** — 첫 프레임이 정지 상태에서 무엇을 보여주는가.
  캐러셀 1장과 같은 자리에 놓고 비교할 것
- **타입** — 서체 종류, 화면 폭 대비 헤드라인 크기, 행간, 대문자 여부
- **색** — 배경 톤과 강조색 개수. 우리 규칙(강조 하나, 슬라이드당 3곳 이하)과 어긋나는지
- **자막 리듬** — 한 번에 띄우는 글자 수, 교체 속도, 화면 내 위치
- **모션** — 글자가 어떻게 들어오고 나가는가. 정지 이미지로 옮길 수 있는 부분과 아닌 부분
- **가져올 것 / 안 가져올 것** — 캐러셀(1080x1080 정지)에 이식 가능한 것만 규칙으로 승격.
  릴스 전용 장치는 별도로 표시해 둔다 (현재 파이프라인은 영상을 만들지 않는다)

## instagram.com/p/DcC1v3PAbAV (캐러셀) — 2026-08-30 보관

링크만 남겨둔다. 레퍼런스로 쓸지 아직 정하지 않았고 분석도 하지 않았다.
쓰기로 하면 위 항목과 같은 목록으로 뜯는다.
